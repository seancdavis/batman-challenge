[build]
  command = "npm run build && npm run generate-redirects"
  publish = "dist"

[build.environment]
  NODE_VERSION = "20"

# Redirects are generated dynamically via generate-redirects script
# to support environment variable interpolation for the Neon Auth proxy
